{% extends 'base.html.twig' %}

{% block title %}Hello ProductsController!{% endblock %}

{% block body %}



    <h3>Products</h3>
    <div class="container">
    <div class="row">
        {% for products in products %}

        <div class="col-md-4 mb-4">


            <div class="card" style="width: 18rem;">
                <img src="{{ asset(products.image) }}" alt=""  class="img-fluid" style="max-width: 250px">
                <div class="card-body">
                    <div class="card-title h5">{{ products.name }}</div>
                    <p class="card-text"><div class="price bold ">€{{ products.price }}</div></p>
                    <a href="#" class="btn btn-primary">Go somewhere</a>
                </div>
            </div>
    </div>
        {% endfor %}
    </div>
</div>


{% endblock %}
